# Path finder task for applicants

setting up the environment:

python3 -m venv venv

source venv/bin/activate

pip install -r requirements.txt

Create instance/config.py file

run the debug server:

python run.py
